# Tasks

- [ ] Get Canvas API access (generate an API token)
- [ ] Pull data via the Canvas API for all courses, reading assignments/quizzes/activities uniformly
- [ ] Format the to-do list output
- [ ] Build the AppleScript (`osascript`) piece that writes/updates Notes.app
- [ ] Wire the pipeline behind a single CLI command
- [ ] Test against one real course first, then expand to all courses
